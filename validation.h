#include "stdio.h"
#include "precision.h"
void ValidateAtomicOperationMax(int numVolumes, const fpkind * pressureLocalMax);
void ValidateAtomicOperationAdd(int numVolumes, const fpkind * hostResidual);
